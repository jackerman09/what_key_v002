<li id="NoteID<%= note.id %>" >
  <span class="content"><%= note.name %> (<%= note.description %>) (ID: <%= note.id %>)</span>
  <span class="timestamp">
<!--     Created <%= time_ago_in_words(note.created_at) %> ago. -->
  </span>
  <span class="content">
  	<%= link_to "Remove", note_path(:id => note.id), :method => :delete %>
  </span>
</li>